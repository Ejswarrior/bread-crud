const React = require('react')
const Default = require('./layouts/Default')
index (() => {
    return (
        <Default>
            <h2>Index Page</h2>
        </Default>
    )
})

module.exports = Index